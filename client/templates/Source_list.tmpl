template SourceList(List source_list) {
	    <ul id="source_lst">
	      ${#each source_list item}
		      <li><span><img src="${item.imageUrl}">message</img>${item.title}</span></li>
	      ${/each}
	    </ul>
}